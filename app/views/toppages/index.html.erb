<% if logged_in? %>
  <p>ログイン中です: <a href="/tasks"><%= current_user.name %>タスク一覧</a></p>
<% else %>
<div class="center jumbotron">
  <div class="text-center">
    <h1>Welcome to the UserTasklist</h1>
    <%= link_to 'Sign up now!', signup_path, class: 'btn btn-lg btn-primary' %>
  </div>
</div>
<% end %>
